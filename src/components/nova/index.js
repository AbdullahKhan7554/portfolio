/**
 * Nova — public entry point. Import the widget (and, if needed, the default
 * config) from here; internal structure stays private.
 */
export { NovaWidget } from './NovaWidget';
export { novaConfig } from '@/config/nova.config';
