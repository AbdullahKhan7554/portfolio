# Milestone 4 — OpenAI + Streaming

## Goal

- Connect OpenAI as first provider; stream real replies (conversation only)

## Completed

- OpenAI adapter (fetch/SSE, no SDK); implements `generate`/`stream`
- Streaming API route with abort + graceful errors
- Knowledge-grounded system prompt from active company
- UI streams tokens; typing indicator; stop/cancel
- Rate-limit + retry are placeholders

## Key modules

- `src/app/api/nova/chat/route.js`
- `src/lib/nova/chat/` — `chatService`, `knowledgePrompt`, schema, placeholders
- `src/lib/nova/providers/openaiProvider.js`
- `src/lib/env.js` — server-only keys

## Git

- Commit: `<M4-commit-sha>`

## Next

- Lead capture / memory / analytics (future)
