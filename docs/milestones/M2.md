# Milestone 2 — AI Foundation

## Goal

- Provider-agnostic AI architecture (no provider connected)

## Completed

- Adapter pattern + provider factory (DI)
- Conversation/message/lead domain models + events + errors
- Prompt fragments, context/system-prompt builders
- In-memory store; token estimator; validators
- Generation left as an explicit unimplemented seam

## Key modules

- `src/lib/nova/providers/` — `baseProvider`, `providerFactory`, adapters
- `src/lib/nova/core/` — engine, conversation, context, prompt builders
- `src/lib/nova/types/` · `config/aiConfig.js` · `utils/`

## Git

- Commit: `<M2-commit-sha>`

## Next

- M3 — Knowledge Management System
